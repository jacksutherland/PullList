using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace PullList.Models
{
    public enum NavSection { Home = 1, Login = 2, About = 3, PullList = 4, Employee = 5 }

    public enum SideNav { None = 0, Store = 1 }
}